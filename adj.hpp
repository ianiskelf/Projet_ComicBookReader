#ifndef ADJ_HPP
#define ADJ_HPP

#include <iostream>
#include <vector>
#include <string>

// Une fonction adjudante qui va nous aider à déterminer le type des fichiers qu'on rencontre
bool extension(const std::string& str, const std::string& suffix);

#endif